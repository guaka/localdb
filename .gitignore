/target/
# Local coverage report output (cargo llvm-cov); CI regenerates its own.
lcov.info
.claude/worktrees/
.omo/
.claude/tmp/
# Local CodeGraph index — a rebuildable cache of this checkout, per-machine
# and large. Whether to index at all is each contributor's own choice.
.codegraph/

# Fetch-on-demand PDF corpus file (scripts/fetch_test_pdfs.sh): free to
# download but not redistributable, so not vendored. The redistributable
# corpus PDFs and all expectation JSONs in that dir ARE committed.
/extract/tests/fixtures/corpus/sewtha-sustainable-energy.pdf
