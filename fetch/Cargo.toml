[package]
name = "localdb-fetch"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "HTTP URL fetcher implementation for localdb ingestion"

[lib]
# Keep the short crate name in code (`use fetch::…`); only the
# package name is namespaced (crates.io collision hygiene — see docs/release-engineering.md).
name = "fetch"

[dependencies]
localdb-core = { path = "../core", version = "0.1.0" }
async-trait = { workspace = true }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
# Real dependency, not just dev: `destination::GuardedResolver` resolves names
# itself via `tokio::net::lookup_host` (`reqwest::dns::Resolve` has no
# delegate-to-default hook).
tokio = { workspace = true }
tracing = { workspace = true }
backon = { workspace = true }
governor = { workspace = true }
httpdate = { workspace = true }

[dev-dependencies]
wiremock = "0.6"
