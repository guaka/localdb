title: localdb
description: Local-first knowledge search with cited retrieval for files and AI assistants.
markdown: kramdown
theme: minima

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

defaults:
  - scope:
      path: ""
    values:
      layout: default
